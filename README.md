# ⚡ zeus

## Install

Setup Virtualenv:

```bash
pipenv --three install
pipenv shell
```

Initialize Database:

```bash
make init_db
```

Run application:

```bash
make run_app
```

## Docker

```bash
docker-compose up
```

Site will be running live on http://localhost:8000
