# ⚡ zeus

## Install

Setup Virtualenv:

```bash
python3 -m venv venv
. venv/bin/activate
```

Install Dependencies:

```bash
pip install -r requirements.txt
```

Initialize Database:

```bash
make init_db
```
